# lunaNMR v1.1 - Python Dependencies
# Install with: pip install -r requirements.txt

# Core scientific computing
numpy>=1.20.0
pandas>=1.3.0
matplotlib>=3.5.0
scipy>=1.7.0
scikit-learn>=1.0.0

# Performance optimization - REQUIRED for 2D multi-peak fitting
# Provides 3-5× speedup for overlapping peak clusters (object mode with scipy.special.wofz)
# Full 10-15× speedup would require porting PINT's C++ Faddeeva (~2-3 hours)
# REQUIRED: System will not function properly without Numba acceleration
numba>=0.57.0  # REQUIRED: 3-5× speedup for 2D multi-peak fitting

# Graph processing for peak detection
networkx>=2.5

# NMR data file reading
nmrglue>=0.9.0

# System monitoring (optional - for performance tracking)
psutil>=5.8.0

# GUI framework (usually included with Python)
# tkinter is part of Python standard library
# Note: On some Linux distributions, tkinter may need to be installed separately:
#   Ubuntu/Debian: sudo apt-get install python3-tk
#   CentOS/RHEL/Fedora: sudo yum install tkinter or sudo dnf install python3-tkinter

# Note: The following are Python standard library modules (no installation needed):
# - os, sys, time, threading, subprocess, json, glob, warnings
# - logging, collections, itertools, functools, pathlib, datetime
# - typing, dataclasses, enum, ast, pickle, copy, inspect, math
# - multiprocessing, shutil, traceback, tkinter

# Development and testing (optional)
# pytest>=6.0.0
# jupyter>=1.0.0
# black>=22.0.0
# flake8>=4.0.0
# mypy>=0.950
